bot:
  name: "Music Finder Bot"
  description: "Telegram bot for searching and sending music files."
  language: "en"
  version: "1.0.0"
  admins:
    - 7971307520  # ID
messages:
  start: |
    Hello, <b>{user}</b>! 👋
    Welcome to Music Finder Bot 🎵
    Send me the name of any song — and I will find it in the database.

  help: |
    <b>Available commands:</b>
    /start — greetings
    /help — this help message
    /artist &lt;name&gt; — get artist info from Genius
    /stats — show database statistics

    Just send any song name to search in the database.

  help_admin: |
    <b>👑 Admin commands:</b>
    /upload — enable upload mode (send audio files to add to database)
    /cancel — cancel upload mode
    /stats — database statistics

  about: |
    🎵 <b>Music Finder Bot v{version}</b>

    This bot helps you find music and information about artists.

    <b>Features:</b>
    • Search for music in database
    • Get detailed artist information
    • View popular songs

    Made with ❤️ using Aiogram 3

  unknown_command: "I don't know this command yet 😅"
  processing: "Searching for your track, please wait… 🔎"
  not_found: "Sorry, I couldn't find this song 😔"
  error: "Unexpected error occurred. Try again later."

  # Сообщения для загрузки треков
  upload:
    mode_enabled: |
      📤 <b>Upload mode enabled!</b>

      Send me audio files to add them to the database.
      I will extract metadata automatically.

      Send /cancel to exit upload mode.

    mode_disabled: "❌ Upload mode disabled."

    success: |
      ✅ <b>Track added to database!</b>

      🎵 <b>Title:</b> {title}
      👤 <b>Artist:</b> {artist}
      🎼 <b>Genre:</b> {genre}
      ⏱ <b>Duration:</b> {duration}
      🆔 <b>Track ID:</b> {track_id}

    invalid_audio: "❌ Please send a valid audio file."
    not_admin: "⛔️ You don't have permission to upload tracks."
    error: "❌ Error while saving track to database. Try again."

  # Сообщения для поиска
  search:
    found_multiple: |
      🎵 <b>Found {count} tracks:</b>

      {results}

      Send the number to get the track.

    found_single: "🎵 Found track: <b>{title}</b> by <b>{artist}</b>"

    no_results: |
      ❌ No tracks found for "<b>{query}</b>"

      Try a different search query or check spelling.

  # Статистика
  stats:
    info: |
      📊 <b>Database Statistics</b>

      🎵 Total tracks: {total_tracks}
      👥 Unique artists: {unique_artists}
      🎼 Genres: {genres_count}
      📅 Last upload: {last_upload}

  # Сообщения для артистов
  artist:
    usage: |
      ℹ️ <b>Usage:</b>
      /artist <b>&lt;artist name&gt;</b>

      <b>Examples:</b>
      /artist The Weeknd
      /artist My Bloody Valentine
      /artist Radiohead

    searching: "🔍 Searching for <b>{artist}</b> on Genius..."
    not_found: |
      ❌ Artist <b>{artist}</b> not found on Genius.

      Try a different spelling or check the artist name.

    api_unavailable: |
      ⚠️ <b>Genius API is currently unavailable</b>

      Artist search feature is temporarily disabled.
      Please contact the administrator.

    error: |
      ❌ An error occurred while fetching artist info.
      Please try again later.

search:
  max_results: 5
  fuzzy_search: true
  case_sensitive: false

database:
  url: "postgresql+asyncpg://musicbot:musicpass@db:5432/musicdb"
  pool_size: 10
  max_overflow: 20
  echo: false  # Логировать SQL запросы (для отладки)

file_limits:
  max_file_size_mb: 20
  supported_formats:
    - mp3
    - wav
    - flac
    - m4a
    - ogg

features:
  send_audio: true
  inline_search: false
  upload_enabled: true
  artist_search: true

# Настройки для Genius API
genius:
  enabled: true
  max_songs: 5
  max_description_length: 600
  include_alternate_names: true
  include_social_links: true
  include_stats: true
  text_format: "plain"

logging:
  level: "INFO"
  log_to_console: true
  log_to_file: true
  file_path: "logs/bot.log"
  max_file_size_mb: 10
  backup_count: 5
  format: "detailed"
